interface:
  display_name: "Docx"
  short_description: "Creating, reading, editing, formatting, merging, converting, inspecting, or generating Word .docx documents with headings, tables, page numbers, letterheads, or other document..."
  default_prompt: "Create, reading, editing, formatting, merging, converting, inspecting, or generating Word .docx documents with headings, tables, page numbers, letterheads, or other document."
